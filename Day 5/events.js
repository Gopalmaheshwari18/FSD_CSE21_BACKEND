//Events Module in jS
// Event class uses  
// on() : register event as listener  
//emit() : trigger event (create and fire both happening due to this)

const EventEmitter = require('events');
const event = new EventEmitter();
